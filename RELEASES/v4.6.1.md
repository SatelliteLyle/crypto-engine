# Release v4.6.1

Release candidate for v4.6.1 — hotfix for live price rendering and WebSocket resiliency.

Commit: fix: correct template literals for price display, add websocket error/close handlers
Branch: fix/price-template-ws-handlers

## Fixes
- Corrected malformed template literals preventing Coinbase and Chainlink price rendering in the UI.
- Added WebSocket error and close handlers to update connection status and log errors.
- Hardened mobile vibration check to avoid runtime exceptions on unsupported clients.

## Verification checklist
- [ ] Coinbase price displays correctly on desktop/laptop
- [ ] Chainlink price updates every 60s
- [ ] Divergence warning displays when prices differ > 0.4%
- [ ] WebSocket status updates to ERROR/DISCONNECTED on failures
- [ ] Mobile vibration triggers on bullish surge (>=75%)
- [ ] GitHub Pages deploys the updated index.html (if enabled)

---

Status: committed to `main` in the crypto-engine repository. The corrected index.html was copied to the separate Pages repository `SatelliteLyle/crypto-hub` to enable a quick Pages deployment for verification.

- crypto-engine commit (fix applied): `7fd9e0fc9b1dcbf496240e70193ccd52bca46ea3`
- Deployed/updated index.html (copied from crypto-engine): https://github.com/SatelliteLyle/crypto-hub/commit/5d76219d450a4bdb295f77c342c14a5c1efffa35

Branching / PRs
- A branch with the source fix exists: `fix/price-template-ws-handlers` (points at the fix commit in `main`).
- I created a documentation branch for this release-note update: `pr/fix-release-notes-update` (this branch contains the updated RELEASES/v4.6.1.md).

Next steps (PR & verification)
1. Open a Pull Request from `fix/price-template-ws-handlers` → `main` in `SatelliteLyle/crypto-engine` for code review. (The release notes update is on `pr/fix-release-notes-update` if you prefer to include it in the PR or merge separately.)
2. Verify Pages deployment at the repository Pages URL (Settings → Pages) and confirm index.html renders and prices update as expected.
3. When verification is complete, check the verification boxes above and merge the PR.

Notes: I have not deleted or renamed any files in `crypto-hub` because I could not find a duplicate file named `index (2).html` in the repository root — only `index.html` is present. If you want me to remove or rename a different path (or the file exists on another branch), provide the exact path and I will act on it.
